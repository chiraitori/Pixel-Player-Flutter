/// Port contract for:
/// `PixelPlayer/.../presentation/screens/AiUsageComponents.kt`.
///
/// Must render source usage meters, limits, status/error states, refresh
/// actions, and provider-backed quota values without fabricated usage.
library;
