/// Port contract for:
/// `PixelPlayer/.../presentation/screens/LibrarySongsTab.kt`.
///
/// Owns song list layout, sort/display modes, selection, playback-aware rows,
/// fast scrolling, list state restoration, and empty/loading states.
library;
