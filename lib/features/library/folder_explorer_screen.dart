/// Port contract for:
/// `PixelPlayer/.../presentation/screens/FolderExplorerScreen.kt`.
///
/// Must preserve folder hierarchy, breadcrumb navigation, filtering, sorting,
/// playback actions, selection state, empty/error states, and storage access.
library;
